target uC {
}

import Led from "./lib/Led.lf"

preamble {=
  #include "reactor-uc/reactor-uc.h"
=}

@platform("esp-idf")
@timeout(10s)
main reactor {
  timer t1(0, 100 msec)
  led = new Led()

  reaction(t1) -> led.tog {=
    lf_set(led.tog, true);
  =}
}
